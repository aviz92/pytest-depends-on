[pytest]
log_cli = true
log_cli_level = DEBUG

addopts =
;    --collect-only
    -s
    -v
    -ra
    --strict-markers
    --tb=short

    --depends-on
    --depends-on-reorder


markers =
    depends_on: Marker for tests that depend on other tests
